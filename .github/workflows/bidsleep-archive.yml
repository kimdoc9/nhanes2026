name: BID-Sleep Phase 5-6 Archive Streaming (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired after measured transfer speed showed the 6.35-GB single stream could not complete within the workflow limit.'
