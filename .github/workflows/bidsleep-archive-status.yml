name: BID-Sleep Archive Status Diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'One-shot diagnostic completed; remote ZIP Range execution is now authoritative.'
