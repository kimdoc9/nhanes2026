name: BID-Sleep Phase 5-6 Public (Superseded)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by bidsleep-phase4-6-accelerated.yml'
